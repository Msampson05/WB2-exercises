// Tip amount 

var foodBill = 40;
var tip_percentage = 15;

// Calculations 

var tip_amount = 40 * (tip_percentage/100)
// Output 

console.log("The tip on a $ " + foodBill + "food bill is $" + tip_amount.toFixed(2));